{% if field.value %}
	<div class="card-image-preview" data-image-url="{{ field.value }}">
		<img src="{{ field.value }}" alt="Card image" style="max-width: 100px; max-height: 100px; border: 1px solid #ddd;"/>
	</div>
{% else %}
	<small style="color: #999;">Aucune image</small>
{% endif %}
