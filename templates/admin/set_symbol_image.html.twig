{% if field.value %}
    <img src="{{ field.value }}" alt="Set symbol" style="max-width: 50px; max-height: 50px; border: 1px solid #ddd;" />
{% else %}
    <small style="color: #999;">-</small>
{% endif %}